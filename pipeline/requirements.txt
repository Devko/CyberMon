# Runtime dependencies (everything else is stdlib). Pinned exactly: the
# nightly must not change behavior because a runner image resolved a
# newer wheel — bump deliberately, with a test run, and mirror the pin in
# the root pyproject.toml.
requests==2.34.2

# Test/dev-only dependencies live in requirements-dev.txt.
