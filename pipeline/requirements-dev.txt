# Development/CI-only dependencies. Runtime deps stay in requirements.txt.
# Pinned exactly (mirrored in the root pyproject.toml's `dev` extra); bump
# deliberately, with a test run.
pytest==9.1.1            # pipeline test suite (pipeline/tests/)
playwright==1.59.0       # headless-browser site smoke test (tools/site_smoke.py);
                         # browsers install separately: playwright install chromium
imageio-ffmpeg==0.6.0    # bundles a STATIC ffmpeg binary for tools/make_motion.py.
                         # Deliberately not `apt install ffmpeg`: the wheel pins one
                         # encoder across local machines and CI, so a clip renders the
                         # same everywhere instead of tracking whatever the runner
                         # image happens to ship.
